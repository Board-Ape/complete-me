import { expect, assert } from 'chai';
import index from '../scripts/index'
import filterMe from '../scripts/index.js'


describe('Example Test File', () => {
  it('should be a function', () => {
    assert.isFunction(filterMe)
  })

})
